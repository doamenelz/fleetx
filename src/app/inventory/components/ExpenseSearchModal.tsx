import { FC } from "react";

export const ExpenseSearchModal: FC<{ vehicleId?: string }> = ({
  vehicleId,
}) => {
  return <></>;
};

/**
 * Search Field
 * Search Button
 * Show Expenses List
 * Click to show details
 */
